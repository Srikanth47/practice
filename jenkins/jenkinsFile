pipeline {
    agent any
     parameters {
        string(name: 'GIT_REPO', defaultValue: 'https://github.com/Srikanth47/practice.git', description: 'GitHub Repository URL')
        choice(name: 'FILE_SELECTION', choices: [], description: 'Choose a file from the repository')
        choice(name: 'topic_operation', choices: ['create_topic', 'delete_topic', 'update_partition'], description: 'Choose a file from the repository')
    }
    stages {
        stage('Fetch Files') {
            steps {
                script {
                    // Clone the GitHub repository
                    echo 'hi'
                }
            }
        }
    }
}
